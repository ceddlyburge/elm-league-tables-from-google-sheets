{
  "name": "ceddlyburge-elm-league-table-from-sheet",
  "description": "A website to calculate and show league tables based on league results entered in a Google Sheet",
  "version": "0.0.1",
  "license": "MIT",
  "author": "Cedd Burge",
  "repository": {
    "type": "git",
    "url": "https://github.com/ceddlyburge/elm-league-table-from-sheet"
  },
  "scripts": {
    "postinstall": "elm-install && cd tests && elm-install && cd .. && cd EndToEndTests && elm-install",
    "test": "elm-test && cd EndToEndTests && bash end-to-end-tests.sh",
    "build": "npm run test && elm-app build"
  },
  "devDependencies": {
    "create-elm-app": "^1.10.4",
    "elm-github-install": "^1.6.1",
    "elm-spec": "^1.1.2"
  },
  "dependencies": {}
}
